import ClueRoom
class Player:
	"""Class to container player, function for player actions, and states
	"""
	def __init__(self, name: str, startingPos: int):
		self._name = name
		self._startingPos = startingPos  #change to object later
		# add initial move since theres special rules for that

	def _canMove():
		return self._position.isBlocked()

	#def _checkMove(nextPos):
		# for move specified, is it actually possible


	#def movePos(nextPos: int)
		#update fields
